import type { Metadata } from "next";
export const metadata: Metadata = { title: "Discover athletes", description: "Find compatible athletes and training partners." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
